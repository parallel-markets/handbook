source "https://rubygems.org"
gem 'github-pages', group: :jekyll_plugins
gem "jekyll-minifier", "~> 0.1"
# when upgrading, ensure this version matches `remote_theme` in _config.yml
gem "just-the-docs", "0.6.2"

group :development do
  gem "webrick"
end
