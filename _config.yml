# Site settings
title: Parallel Handbook

# Build settings
markdown: kramdown
remote_theme: just-the-docs/just-the-docs@v0.6.2
color_scheme: parallel
baseurl: /handbook

plugins:
  - jekyll-minifier
  - jekyll-github-metadata
  - jemoji

permalink: pretty

jekyll-minifier:
  uglifier_args:
    harmony: true

search_enabled: true
aux_links:
  "Parallel Home": "https://about.parallelmarkets.com"

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - Makefile
  - README.md
